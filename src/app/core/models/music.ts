export class Music {
  constructor(
    public name: string,
    // tslint:disable-next-line:variable-name
    public preview_url: string,
    // tslint:disable-next-line:variable-name
    public artist_name: string,
    // tslint:disable-next-line:variable-name
    public itunes_id: number,
    // tslint:disable-next-line:variable-name
    public artwork_url: string,
    // tslint:disable-next-line:variable-name
    public collection_name: string,
  ) {
  }
}
